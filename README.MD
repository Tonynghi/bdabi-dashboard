# BDABI Dashboard

This project is a simple Streamlit dashboard application. It demonstrates how to create a basic web app using Streamlit and Python. The app displays a title and a greeting message.

## How to Run

1. Make sure you have Python installed on your system.
2. Install Streamlit if you haven't already:
   ```powershell
   pip install streamlit
   ```
3. Run the application using the following command in your project directory:
   ```powershell
   streamlit run app.py
   ```
4. Your default web browser will open with the dashboard.

## File Structure

- `app.py`: Main Streamlit application file.
- `README.MD`: Project documentation and instructions.
